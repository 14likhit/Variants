<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.likhit.variants">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_variant_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_variant_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">
        <activity
            android:name=".ui.home.HomeActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.companies.CompanyActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.jobs.JobActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.locations.LocationActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>